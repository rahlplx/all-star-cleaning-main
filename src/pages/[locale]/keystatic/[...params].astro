---
import { makePage } from '@keystatic/astro/ui';
import config from '../../../../keystatic.config';

export const prerender = false;
export const all = makePage(config);

const { params } = Astro.params;
---

{all}
